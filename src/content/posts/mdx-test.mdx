---
title: "MDX Test: Code & Math"
date: 2025-09-01
lang: "en"
translationOf: ""
description: "Testing MDX rendering with code fences and KaTeX math."
tags: ["mdx","math","code","test"]
---
import CodeWithCopy from '../../components/CodeWithCopy.astro';

This is a short MDX test post to verify:

- code highlighting (Shiki),
- inline and block math (KaTeX),
- normal Markdown rendering.

Inline math example: Euler's identity is $e^{i\pi} + 1 = 0$.

Block math example:

$$
\int_{0}^{\infty} e^{-x^2}\,dx \;=\; \tfrac{\sqrt{\pi}}{2}
$$

Here's a JavaScript code block (should be highlighted):

<CodeWithCopy code={`function add(a, b) {
  return a + b;
}
console.log(add(2, 3));
`} lang="js" theme="github-dark" />

